# Changelog

## 0.1.0

First release.

- A window showing the avatar's finished menu, read back from a real build.
- Paging at a chosen limit of 2–8 items per page, replacing Modular Avatar's,
  for the root wheel or for every submenu.
- A remembered order per menu, matched across builds by parameter and label.
